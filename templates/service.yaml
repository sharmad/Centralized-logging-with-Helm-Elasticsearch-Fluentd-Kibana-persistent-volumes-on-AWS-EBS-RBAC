apiVersion: v1
kind: Service
metadata:
  name: {{ template "mylogging.fullname" . }}
  labels:
    app: {{ template "mylogging.name" . }}
    chart: {{ template "mylogging.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "mylogging.name" . }}
    release: {{ .Release.Name }}
